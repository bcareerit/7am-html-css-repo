Positions
*********
CSS supports

    1) absolute
    2) relative
    3) fixed
    4) sticky
    5) static

absolute
    child element movement depending on parent element movement

    because of absolute position, other elements will effect on layout

    syntax.
            position : absolute;
            left :  
            right :
            top : 
            bottom : 

CSS Dimensions
**************
    CSS supports following Dimensions

    1) px (pixel)
    2) rem (root em).   1rem = 16px
    3) em
    4) percentage. (%)
    5) vh(viewport height) && vw(viewport width)

rem
***
    rem stands for root em
    1rem = 16px
    all html elements Dimensions depending on root element Dimension
    if root element(<html></html>) Dimension changes automatically all elements(<p>,<div>,<form>,....) Dimension also changes

em
**
    child element Dimension depending on parent element Dimension

% (percentage)
**************
    used to measure with respect to screen
    Ex.
        10%
        50%
    
    child element dimension, depending on parent element dimension

vw (viewport width).  && vh (viewport height)
*********************************************

     total width of the screen called as 100vw
     total height of the screen called as 100vh

    html elements dimensions depending on screen


CSS Flex
********
    CSS Flex, used to move elements with respect to main axis
    CSS Flex is one dimensional
    CSS Flex by default Responsive
    Ex.
        display : flex;
    in CSS Flex, parent and chid relationship mandatory

1) display : flex;

2) gap

3) justify-content : flex-start / center / flex-end / space-between / space-around / space-evenly;

    flex-start is the default value

4) align-items : flex-start / center / flex-end / stretch;

    stretch is the default value

5) flex-direction : row / row-reverse / column / column-reverse;

    row is the default value

6) flex-wrap : wrap / wrap-reverse / nowrap;

    nowrap is the default value

7) order

    order, used to change the order of flex items

pseudo classes
**************
    set special state to element
    we will apply pseudo classes with ":"
    Ex.
        :hover
        :first-of-type
        :last-of-type
        :first-child
        :last-child
        :nth-child
        :checked
        :focus
        :not

pseudo elements
***************
    pseudo elements are used to style the portion of element
    we will apply pseudo elements with "::"

    Ex.
        ::before
        ::after
        ::first-letter
        ::first-line
        ::selection
        ::marker

Lists
*****
    1) unordered list
    2) ordered list
    3) definition list

    <ul></ul>. ----  unordered list
    <li></li>. ----  list item

    <ol></ol> ----   ordered list

    <dl></dl>. ----- definition list

HTML Introduction
HTML Headings
HTML Paragraphs
HTML Divisions
Tables
Forms
Lists
iframe
fieldset
anchor
                            marquee
images

CSS

CSS Introduction
apply styles
    external
    internal
    inline
css specificity
inline > id > class > element > universal

selectors
    element selector
    id selector
    class selector
    decendent selector
    attribute selector
    universal selector (*)

pseudo classes & pseudo elements

css flex

css grid

css media queries (partial)

dimensions
    rem
    em
    px
    %
    vw & vh

positions

*************************************************************************************
1) semantic elements. (<header></header>,......,<audio></audio>, <video></video>,....)

2) media queries. (min-width, max-width, screen && break points)

3) box-sizing : border-box;  // 

4) revise flex concept. & show deploy


box-sizing
**********
div{
    width : 100px;
    height : 100px;
    border : 1px solid grey;
    padding:20px;
    box-sizing: border-box;
}
width : 102px;  98px;              width : 142px;   58px;
height : 102px; 98px;            height : 142px;    58px;

border-box
**********
original width includes padding and border

content-box
***********
original width and height excludes border and padding









































